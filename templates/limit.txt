# JagofTrade Limit File
# This file is for internal reference only.

Max crawl depth: 3
Max page size: 2MB
Restricted directories: /admin/, /private/
Preferred sitemap: /sitemap.xml

# limit.txt — Production resource limits for Django

# Worker processes
WEB_CONCURRENCY=4

# Max requests per worker before recycling (helps prevent memory leaks)
MAX_REQUESTS=1000
MAX_REQUESTS_JITTER=50

# Memory limits (MB)
DJANGO_MAX_MEMORY=512
DJANGO_MIN_MEMORY=128

# Timeout settings (seconds)
GUNICORN_TIMEOUT=30
GUNICORN_GRACEFUL_TIMEOUT=20

# Thread limits
THREADS=2

# Database connection pool
DB_CONN_MAX=20
DB_CONN_MIN=5

# Static/media file limits
UPLOAD_MAX_SIZE=10485760   # 10 MB
UPLOAD_TIMEOUT=60

# Security limits
SESSION_COOKIE_AGE=1209600   # 2 weeks
CSRF_COOKIE_SECURE=True
SESSION_COOKIE_SECURE=True